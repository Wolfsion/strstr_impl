#ifndef _DS_H_
#define _DS_H_

#include<vector>

using namespace std;

const int SIZE = 26;

BitMap::BitMap(size_t num);
BitMap::set(size_t num);
BitMap::reSet(size_t num);
BitMap::hasExisted(size_t num);

#endif